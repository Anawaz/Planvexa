<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Planvexa.Modules.Reporting</RootNamespace>
    <AssemblyName>Planvexa.Modules.Reporting</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\BuildingBlocks\Planvexa.BuildingBlocks\Planvexa.BuildingBlocks.csproj" />
    <ProjectReference Include="..\..\..\SharedContracts\Planvexa.SharedContracts\Planvexa.SharedContracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" />
    <PackageReference Include="PDFsharp" />
  </ItemGroup>

  <ItemGroup>
    <!-- This module's CsvWriter is internal (a pure utility, not part of the module's contract) but its
         formula-injection escaping is security behaviour that needs a direct test rather than one
         inferred through a service that also needs a database. -->
    <InternalsVisibleTo Include="Planvexa.UnitTests" />
  </ItemGroup>

</Project>
