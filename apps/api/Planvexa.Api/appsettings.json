{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Planvexa": "Host=localhost;Port=5432;Database=planvexa;Username=planvexa;Password=planvexa",
    "PlanvexaMaintenance": ""
  },
  "Database": {
    "RunDbUpOnStartup": true,
    "SeedDevelopmentData": false,
    "ResetDevelopmentData": false
  },
  "Authentication": {
    "UseDevelopmentHeaders": false
  },
  "//Registration": "Seed default only. On first read the value moves into platform.instance_settings and a host administrator edits it in the host console from then on; changing it here afterwards has no effect.",
  "Registration": {
    "AllowSelfRegistration": false
  },
  "//HostAdmin": "Break-glass only. Identity-provider subjects listed here reach the host console even without the identity.users.is_host_admin flag, so an installation whose last host administrator was lost is recoverable. Normally empty; the first host admin is created from Bootstrap:AdminSubject. Also accepts a comma-separated HostAdmin__Subjects environment variable.",
  "HostAdmin": {
    "Subjects": []
  },
  "//InstanceLogs": "The host console's persisted log store (platform.instance_logs). MinimumLevel is deliberately Warning, not Information: these records are persisted and may contain user data, and the OpenTelemetry pipeline already carries the full stream. RetentionDays is what bounds how long that data lives.",
  "InstanceLogs": {
    "Enabled": true,
    "MinimumLevel": "Warning",
    "RetentionDays": 14,
    "Capacity": 2000
  },
  "Distribution": {
    "DistributionName": "Planvexa",
    "DistributionMaintainer": "Planvexa",
    "DistributionSourceUrl": "https://github.com/Anawaz/Planvexa",
    "IsOfficialDistribution": true,
    "UpstreamProjectName": "Planvexa",
    "UpstreamProjectUrl": "https://github.com/Anawaz/Planvexa",
    "SourceCodeUrl": "https://github.com/Anawaz/Planvexa/tree/main",
    "LicenseIdentifier": "AGPL-3.0-only",
    "CopyrightOwner": "Planvexa contributors"
  },
  "//Keycloak:Admin": "OPTIONAL. Self-registration has two halves: the host console's toggle decides whether Planvexa ACCEPTS a new identity, while Keycloak decides whether an account can be CREATED at all. Supply admin credentials and the toggle drives both; leave them blank and the console warns that you must also enable registration in Keycloak (Realm settings -> Login -> User registration). Prefer AdminClientId/AdminClientSecret: a confidential client whose service account holds manage-realm, scoped to one realm and revocable, rather than the master admin password.",
  "Keycloak": {
    "Authority": "http://localhost:8081/realms/planvexa",
    "Audience": "planvexa-api",
    "AdminClientId": "",
    "AdminClientSecret": "",
    "AdminUser": "",
    "AdminPassword": ""
  },
  "OpenTelemetry": {
    "OtlpEndpoint": ""
  },
  "Smtp": {
    "Host": "",
    "Port": 1025,
    "From": "no-reply@planvexa.local"
  }
}
