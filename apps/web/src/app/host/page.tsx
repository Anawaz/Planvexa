import { OverviewPageClient } from "@/components/host/OverviewPageClient";

export const metadata = { title: "Host administration" };

export default function HostOverviewPage() {
  return <OverviewPageClient />;
}
