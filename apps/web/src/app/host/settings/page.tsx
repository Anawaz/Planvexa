import { SettingsPageClient } from "@/components/host/SettingsPageClient";

export const metadata = { title: "Instance settings · Host administration" };

export default function HostSettingsPage() {
  return <SettingsPageClient />;
}
