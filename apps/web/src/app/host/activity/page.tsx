import { ActivityPageClient } from "@/components/host/ActivityPageClient";

export const metadata = { title: "Activity · Host administration" };

export default function HostActivityPage() {
  return <ActivityPageClient />;
}
